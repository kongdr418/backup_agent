# 智创空间交互式课堂工程规划

> 版本：v1.0
> 日期：2026-06-01
> 目标：先把“交互式课堂”作为真实可运行主链路做扎实，优先复用现有 PPT Studio、SVG PPT 流水线、PPTist 编辑器和微课视频 TTS 能力，再逐步接入学生画像、学习路径和效果评估，形成对标软件杯 A3 的个性化学习闭环。
> 参考：OpenMAIC 的 Stage / Scene / Action / Quiz / Playback 思路，但不直接迁移其 React/Next 代码，也不照搬旧版 `DEVELOPMENT_ROADMAP.md`。

---

## 1. 总体判断

当前“智创空间”的优势已经很明显：

- Flask + Vue3 + Vite 的完整 Web 应用结构已经具备；
- 已有 SVG 多 Agent PPT 生成流水线；
- 已有讲义、习题、课堂测验、思维导图、微课视频等内容生成器；
- 已有 PPTist 编辑器、文件库、模板系统、模型设置、TTS provider 配置；
- 已经接入多种 TTS，其中 Edge TTS 可作为免 API Key 的稳定演示选项；
- 已有 `QuizPlayer.vue`，说明互动答题基础已经存在。

但如果要对标 A3 赛题，项目不能只表现为“教师输入主题，系统生成资源”。评委更希望看到：

```text
学生进入系统
→ 系统理解学生
→ 生成适配的课堂
→ 学生在课堂里听讲、互动、答题
→ 系统记录效果
→ 更新掌握度
→ 推荐下一步学习
```

因此，本阶段不应先做一堆“画像页、路径页、报告页”的前端壳子，而应先做一个真实可运行的核心体验：

```text
后端生成交互式课堂数据
→ 前端进入课堂子页面
→ 播放讲解 Action 和 TTS
→ 插入互动测验
→ 提交答案
→ 后端返回反馈
→ 形成学习记录
```

一句话：**先做真实课堂闭环，再做画像和路径包装。**

---

## 2. OpenMAIC 思路拆解与本项目映射

OpenMAIC 值得学习的是课堂抽象，不是具体技术栈。它的关键产品思路可以拆成五层：

| OpenMAIC 概念 | 产品含义 | 智创空间实现方式 |
| --- | --- | --- |
| Stage | 一节完整课堂，包含多个场景 | `InteractiveClassroom`，一节可播放课堂 |
| Scene | 课堂里的单个场景 | `slide` / `quiz` / `summary` |
| Action | 教师讲解、聚焦、播放、反馈等动作 | `speech` / `spotlight` / `quiz_feedback` |
| Playback | 自动或半自动播放课堂 | Vue 课堂播放器 + Action 执行器 |
| Quiz | 课堂中穿插答题 | 复用并升级 `QuizPlayer.vue` |

OpenMAIC 中暂不建议迁移的部分：

- 白板系统：交互复杂，短期不是刚需；
- 多 Agent 圆桌讨论：演示有趣，但会稀释主线；
- iframe widget / 模拟实验：适合后续增强，第一版不做；
- PBL 项目式学习：可放入第二阶段；
- React 状态管理和 IndexedDB 实现：不适合直接搬到 Vue3 + Flask 项目中。

本项目第一阶段只借鉴 OpenMAIC 的最核心骨架：

```text
课堂 = 多个 Scene
Scene = 内容 + Actions
Action = 可执行教学动作
Quiz = 可评分互动场景
Playback = 按顺序执行课堂
```

---

## 3. 目标体验

第一版交互式课堂应该做到：

1. 用户输入一个主题，例如“Python 循环语句”。
2. 后端生成一节课堂，包括：
   - 开场页；
   - 2-4 个知识讲解页；
   - 1-2 个随堂测验页；
   - 总结页。
3. 每个讲解页都有教师讲解词。
4. 每段讲解词通过现有 TTS provider 生成音频，优先复用微课视频中已经适配的 TTS 逻辑。
5. 前端课堂页面可以：
   - 查看场景列表；
   - 切换场景；
   - 播放教师讲解；
   - 自动播放 TTS；
   - 在测验页答题；
   - 提交后获得即时反馈和解析。
6. 课堂幻灯片优先来自现有 PPT Studio 的 SVG 产物，而不是重新做一套低质量 PPT 生成模块。
7. 后端保存答题结果和学习记录。
8. 完课后显示简要报告：
   - 学了哪些知识点；
   - 答对几题；
   - 哪些知识点需要补强；
   - 推荐下一步学习内容。

这个体验跑通后，再加入学生画像和学习路径，才会非常自然。

---

## 4. 产品边界

### 4.1 第一版必须做

- 真实后端接口；
- 真实课堂 JSON 数据；
- 真实课堂播放器页面；
- 真实 TTS 调用；
- 真实测验提交和评分；
- 真实学习记录保存；
- 至少一个可演示课程主题。

### 4.2 第一版不做

- 登录注册；
- 权限系统；
- 数据库迁移；
- Redis / Celery 任务队列；
- 白板；
- 多 Agent 圆桌讨论；
- 复杂互动 widget；
- 完整课程平台；
- 复杂教师端管理后台。

### 4.3 可以预留但不实现

- `scene.type = "interactive"`；
- `action.type = "whiteboard"`；
- `agent.role = "student"`；
- `resource_links`；
- `knowledge_refs`；
- `learning_path_id`。

---

## 5. 核心数据模型

建议命名不用完全照 OpenMAIC 的 Stage，而是用更贴合项目的 `InteractiveClassroom`。

### 5.1 Classroom

```json
{
  "id": "cls_20260601_001",
  "user_id": "default_user",
  "title": "Python 循环语句交互式课堂",
  "topic": "Python 循环语句",
  "course": "Python 程序设计",
  "status": "ready",
  "created_at": "2026-06-01T10:00:00+08:00",
  "updated_at": "2026-06-01T10:00:00+08:00",
  "tts": {
    "provider": "edge",
    "model": "edge-tts",
    "voice": "zh-CN-XiaoxiaoNeural"
  },
  "agents": [
    {
      "id": "teacher",
      "name": "AI 教师",
      "role": "teacher",
      "avatar": "",
      "color": "#0f766e",
      "persona": "讲解清晰、善于举例、会在关键知识点停顿提问。"
    }
  ],
  "source": {
    "type": "ppt_svg_job",
    "job_id": "ppt_20260601_001",
    "pptist_editable": true
  },
  "knowledge_points": ["while 循环", "for 循环", "range 函数", "break 与 continue"],
  "scenes": []
}
```

### 5.2 Scene

```json
{
  "id": "scene_001",
  "type": "slide",
  "title": "为什么需要循环",
  "order": 1,
  "knowledge_points": ["循环的作用"],
  "content": {
    "format": "svg",
    "svg": "<svg>...</svg>",
    "markdown": "",
    "ppt_slide": {
      "job_id": "ppt_20260601_001",
      "page": 1,
      "filename": "slide_001.svg"
    }
  },
  "actions": []
}
```

测验 Scene：

```json
{
  "id": "scene_quiz_001",
  "type": "quiz",
  "title": "随堂测验：for 循环",
  "order": 3,
  "knowledge_points": ["for 循环", "range 函数"],
  "content": {
    "questions": [
      {
        "id": "q1",
        "type": "single",
        "question": "下面哪段代码会输出 0 到 4？",
        "options": [
          { "label": "for i in range(5): print(i)", "value": "A" },
          { "label": "for i in range(1,5): print(i)", "value": "B" },
          { "label": "for i in range(5,1): print(i)", "value": "C" },
          { "label": "while i < 5: print(i)", "value": "D" }
        ],
        "answer": ["A"],
        "analysis": "range(5) 会依次产生 0、1、2、3、4。",
        "points": 1,
        "knowledge_point": "range 函数"
      }
    ]
  },
  "actions": []
}
```

### 5.3 Action

```json
{
  "id": "act_001",
  "type": "speech",
  "agent_id": "teacher",
  "text": "同学们，循环语句解决的是重复执行的问题。比如我们要打印 100 行内容，不可能手写 100 次 print。",
  "audio_url": "/api/interactive-classroom/cls_001/audio/act_001.mp3",
  "speed": 1.0
}
```

第一版建议支持三类 Action：

| Action | 用途 | 是否必须 |
| --- | --- | --- |
| `speech` | 教师讲解 + TTS | 必须 |
| `spotlight` | 高亮当前重点区域 | 可选 |
| `quiz_feedback` | 答题反馈讲解 | 必须 |

---

## 6. 后端模块设计

### 6.1 新增目录

```text
backend/
├── interactive_classroom/
│   ├── __init__.py
│   ├── schema.py
│   ├── storage.py
│   ├── generator.py
│   ├── quiz_service.py
│   ├── tts_service.py
│   └── sample_data.py
│
├── course_data/
│   └── python_programming/
│       ├── course_outline.json
│       ├── knowledge_points.json
│       └── quiz_bank.json
```

### 6.2 `schema.py`

职责：

- 定义 `InteractiveClassroom`；
- 定义 `ClassroomScene`；
- 定义 `ClassroomAction`；
- 定义 `QuizQuestion`；
- 定义 `AnswerSubmission`；
- 定义 `ClassroomReport`。

第一版可以用 `dataclass`，避免引入 Pydantic 造成依赖变化。

### 6.3 `storage.py`

职责：

- 按用户隔离保存课堂；
- 保存课堂 JSON；
- 读取课堂 JSON；
- 列出课堂；
- 保存答题记录；
- 保存音频文件路径。

建议路径：

```text
backend/memory/users/<user_id>/interactive_classrooms/<classroom_id>/
├── classroom.json
├── answers.json
├── report.json
└── audio/
    ├── act_001.mp3
    └── act_002.mp3
```

### 6.4 `generator.py`

职责：

- 根据主题生成课堂结构；
- 生成 slide Scene；
- 生成 quiz Scene；
- 生成 speech Actions；
- 调用 TTS 生成音频；
- 保存课堂。

第一版生成策略：

1. 优先复用 PPT Studio：
   - 若请求传入 `ppt_job_id`，直接从 `/api/ppt-svg/preview-all/<job_id>` 对应后端逻辑读取 SVG 页；
   - 若未传入 `ppt_job_id`，调用现有 `ppt_engine.pipeline` 生成 SVG PPT job；
   - 每页 SVG 转成一个 `slide` Scene；
   - 保留 `ppt_job_id/page/filename`，后续可跳转 PPTist 精修。
2. 确定性课程数据兜底：
   - 基于 `course_data/python_programming` 的知识点和题库；
   - 根据主题匹配知识点；
   - 在 PPT pipeline 不可用时生成简单 SVG / Markdown slide。
3. 再接 LLM 增强：
   - 讲解词可以由 LLM 生成；
   - 测验题可以先从题库抽取，后续再动态生成；
   - 学习报告和推荐理由可由 LLM 润色。

### 6.5 `tts_service.py`

职责：

- 不新造 TTS provider；
- 优先抽取并复用 `video_generator.py` 和 `/api/tts-test` 中已经跑通的 TTS 适配逻辑；
- 使用用户当前设置中的 `tts_provider / tts_model / tts_voice`；
- Edge TTS 作为推荐默认演示选项；
- 生成失败时保留 `speech.text`，不阻塞课堂创建。

目标接口：

```python
class ClassroomTTSService:
    def synthesize_action(self, action, settings, output_dir) -> str:
        """Return audio_url or empty string."""
```

重点：TTS 是交互式课堂第一版主链路，不作为后期功能。

### 6.6 `quiz_service.py`

职责：

- 校验答案；
- 计算得分；
- 生成反馈；
- 聚合知识点掌握情况；
- 保存答题记录。

返回示例：

```json
{
  "score": 80,
  "correct": 4,
  "total": 5,
  "results": [
    {
      "question_id": "q1",
      "correct": true,
      "your_answer": ["A"],
      "correct_answer": ["A"],
      "analysis": "range(5) 会依次产生 0 到 4。"
    }
  ],
  "knowledge_summary": {
    "range 函数": {
      "correct": 1,
      "total": 1,
      "mastery_delta": 8
    }
  }
}
```

---

## 7. API 设计

### 7.1 生成课堂

```text
POST /api/interactive-classroom/generate
```

请求：

```json
{
  "topic": "Python 循环语句",
  "course": "Python 程序设计",
  "user_id": "default_user",
  "ppt_job_id": "ppt_20260601_001",
  "tts_provider": "edge",
  "tts_model": "edge-tts",
  "tts_voice": "zh-CN-XiaoxiaoNeural"
}
```

响应：

```json
{
  "classroom_id": "cls_20260601_001",
  "status": "ready",
  "classroom": {}
}
```

第一版可以先用普通 JSON 响应；如果生成时间较长，再改成 SSE。

说明：

- `ppt_job_id` 可选；
- 有 `ppt_job_id` 时，交互式课堂直接复用已有 PPT Studio 结果；
- 没有 `ppt_job_id` 时，后端可先创建 SVG PPT job，再包装成课堂；
- 这能避免重新做一套较弱的 PPT 模块。

### 7.2 获取课堂

```text
GET /api/interactive-classroom/<classroom_id>?user_id=default_user
```

返回：

```json
{
  "classroom": {}
}
```

### 7.3 课堂列表

```text
GET /api/interactive-classroom/list?user_id=default_user
```

返回：

```json
{
  "classrooms": [
    {
      "id": "cls_001",
      "title": "Python 循环语句交互式课堂",
      "topic": "Python 循环语句",
      "scene_count": 6,
      "created_at": "2026-06-01T10:00:00+08:00"
    }
  ]
}
```

### 7.4 提交答案

```text
POST /api/interactive-classroom/<classroom_id>/answer
```

请求：

```json
{
  "scene_id": "scene_quiz_001",
  "answers": {
    "q1": ["A"],
    "q2": ["C"]
  },
  "user_id": "default_user"
}
```

响应：

```json
{
  "score": 80,
  "correct": 4,
  "total": 5,
  "results": [],
  "feedback_action": {
    "id": "feedback_001",
    "type": "quiz_feedback",
    "agent_id": "teacher",
    "text": "这次答题整体不错，但 range 函数的边界还需要再练习。",
    "audio_url": "/api/interactive-classroom/cls_001/audio/feedback_001.mp3"
  }
}
```

### 7.5 获取学习报告

```text
GET /api/interactive-classroom/<classroom_id>/report?user_id=default_user
```

返回：

```json
{
  "report": {
    "classroom_id": "cls_001",
    "score": 80,
    "knowledge_mastery_updates": {
      "range 函数": 72,
      "for 循环": 85
    },
    "weak_points": ["range 函数"],
    "next_recommendation": "建议继续学习 break 与 continue，并完成 3 道边界条件练习。"
  }
}
```

---

## 8. 前端页面与组件设计

### 8.1 新增路由

```text
/interactive-classroom
/interactive-classroom/:classroomId
```

后续再加：

```text
/profile-setup
/learning-path
/learning-report/:classroomId
```

### 8.2 页面结构

```text
frontend/src/views/
├── InteractiveClassroomHomeView.vue
└── InteractiveClassroomPlayerView.vue
```

### 8.3 组件结构

```text
frontend/src/components/classroom/
├── ClassroomSceneList.vue
├── ClassroomStage.vue
├── SlideScene.vue
├── QuizScene.vue
├── ActionNarrator.vue
├── ClassroomControls.vue
├── ClassroomReportPanel.vue
└── TeacherBubble.vue
```

### 8.4 页面布局

课堂播放器建议采用三栏或上下结构：

```text
┌────────────────────────────────────────────┐
│ 顶部：课堂标题 / TTS 状态 / 退出课堂        │
├──────────────┬─────────────────────────────┤
│ 左：Scene列表 │ 中：Slide / Quiz 主内容       │
│              │                             │
├──────────────┴─────────────────────────────┤
│ 底部：教师讲解气泡 + 播放控制 + 上一页下一页 │
└────────────────────────────────────────────┘
```

移动端可以降级为：

```text
顶部标题
主内容
讲解区
底部导航
```

### 8.5 `ActionNarrator.vue`

职责：

- 显示当前 `speech`；
- 显示教师头像和名称；
- 自动播放 `audio_url`；
- 支持播放 / 暂停 / 重播；
- 音频失败时显示文本，不阻塞课堂。

### 8.6 `QuizScene.vue`

第一版建议不直接改坏现有 `QuizPlayer.vue`，而是：

- 复用其 UI 和评分逻辑；
- 抽出更通用的题目类型；
- 新组件适配 `InteractiveClassroom` 的 quiz 数据结构。

功能：

- 单选题；
- 提交按钮；
- 正误状态；
- 答案解析；
- 得分统计；
- 调用后端保存结果；
- 收到 `feedback_action` 后交给 `ActionNarrator` 播放。

---

## 9. TTS 设计

TTS 是交互式课堂核心体验，不放到后期。

### 9.1 复用原则

- 不重新设计 TTS provider；
- 复用现有模型设置页中的 TTS provider；
- 复用现有 Edge TTS / GLM-TTS / MiMo-TTS 等适配；
- 课堂生成时读取当前 TTS 设置；
- 每个 `speech Action` 对应一段音频。

### 9.2 推荐默认值

演示默认建议：

```json
{
  "tts_provider": "edge",
  "tts_model": "edge-tts",
  "tts_voice": "zh-CN-XiaoxiaoNeural"
}
```

理由：

- 免 API Key；
- 演示稳定；
- 适合本地答辩环境；
- 可以保留其他 provider 作为可切换能力，体现扩展性。

### 9.3 音频存储

建议音频保存到课堂目录：

```text
backend/memory/users/<user_id>/interactive_classrooms/<classroom_id>/audio/
```

前端播放 `audio_url`，不要把大段 audio base64 放入课堂 JSON，避免本地存储膨胀。

### 9.4 降级策略

TTS 失败时：

- `speech.text` 正常保留；
- `audio_url` 为空；
- 前端显示“语音暂不可用，已切换为文本讲解”；
- 不让课堂生成失败。

---

## 10. 生成策略

### 10.1 第一版：复用 PPT Studio 优先

PPT Studio 当前可用性较好，因此交互式课堂不应绕开它。第一版推荐将 PPT Studio 作为课堂 slide 的主来源。

输入：

```text
topic = Python 循环语句
可选 ppt_job_id = 已生成的 SVG PPT job
```

生成：

```text
PPT Studio SVG 第 1 页 → slide Scene 1 + speech actions
PPT Studio SVG 第 2 页 → slide Scene 2 + speech actions
关键知识点之后插入 → quiz Scene
PPT Studio SVG 最后一页 → summary Scene
```

流程：

1. 用户在 PPT Studio 生成 PPT；
2. 在 PPT 预览页点击“转为交互式课堂”；
3. 后端读取该 `ppt_job_id` 的 SVG 页面；
4. 为每页生成讲解词；
5. 调用 TTS provider 生成音频；
6. 在合适位置插入 Quiz Scene；
7. 保存为 `InteractiveClassroom`。

如果没有现成 PPT job，则可以提供“从主题直接生成课堂”：

```text
主题 → 调用 PPT Studio SVG pipeline → 包装成交互式课堂
```

### 10.2 兜底：确定性课堂生成

当 PPT pipeline 或模型不可用时，才使用确定性生成，保证演示不断：

- 简单 SVG 模板；
- 或 Markdown 卡片渲染；
- 或项目已有 SVG 模板资产。

### 10.3 第二版：借鉴微课视频生成

微课视频已经实现了“PPT/SVG → 讲稿 → TTS → 逐页产物”的思路，交互式课堂可以复用其中两类能力：

- 讲解词生成：参考 `VideoGenerator._generate_llm_scripts`，但输出到 `speech Action`；
- TTS 音频生成：复用 `VideoGenerator._generate_tts_audio` 的 provider 分发；
- 逐页结构：视频是逐页合成，课堂是逐页播放，本质上都需要 page-level script。

区别是：

```text
微课视频：生成不可交互 MP4
交互式课堂：生成可播放、可暂停、可答题、可记录的课堂 JSON
```

### 10.4 第三版：接入现有内容生成器

当基础课堂跑通后，再增强：

- `quiz` 接入现有 `quiz_generator.py`；
- `speech` 接入现有讲稿生成逻辑；
- `summary` 接入学习报告生成；
- `mindmap` 可作为课堂补充资源；
- 课堂结束后可继续进入微课视频生成，导出为 MP4。

### 10.5 第四版：个性化生成

接入学生画像后，根据画像调整：

| 画像特征 | 课堂变化 |
| --- | --- |
| 零基础 | 讲解更慢、术语更多解释、题目更基础 |
| 喜欢图解 | slide 更偏图示和类比 |
| 喜欢代码 | 增加代码片段和调试题 |
| 目标考试 | 增加考点总结和基础题 |
| 目标竞赛 | 增加边界条件、复杂度和拓展题 |

---

## 11. 学生画像与学习路径的接入顺序

学生画像很重要，但不应压过交互式课堂主线。建议顺序：

### 11.1 先做课堂

```text
生成课堂
→ 播放课堂
→ 答题反馈
```

### 11.2 再做轻量画像

第一版画像只需要：

```json
{
  "major": "软件工程",
  "grade": "大二",
  "course": "Python 程序设计",
  "goal": "通过考试",
  "basis": "零基础",
  "cognitive_style": "图解+案例",
  "resource_preference": ["微课", "练习题"],
  "knowledge_mastery": {
    "循环": 40,
    "函数": 20
  }
}
```

### 11.3 再做学习路径

学习路径不用一开始做复杂 DAG，先做阶段列表：

```json
{
  "phases": [
    {
      "id": "phase_1",
      "title": "变量与数据类型",
      "status": "completed",
      "classroom_id": "cls_001"
    },
    {
      "id": "phase_2",
      "title": "条件语句",
      "status": "ready",
      "classroom_id": ""
    },
    {
      "id": "phase_3",
      "title": "循环语句",
      "status": "locked",
      "classroom_id": ""
    }
  ]
}
```

### 11.4 最后做闭环调整

```text
测验得分低
→ 更新 knowledge_mastery
→ 插入补强课堂
→ 推荐下一节
```

---

## 12. 里程碑规划

这里不按死日期排，因为 Codex 辅助开发速度较快。建议按可验收里程碑推进。

### M1：交互式课堂真实跑通

目标：

- 后端能从已有 PPT Studio job 包装出课堂 JSON；
- 无 PPT job 时能按主题创建课堂；
- 前端能打开课堂页面；
- 能切换 Scene；
- 能显示讲解文本；
- 能完成测验并提交；
- 后端能返回评分和解析。

验收：

```text
在 PPT Studio 生成“Python 循环语句”
→ 点击“转为交互式课堂”
→ 生成 classroom_id
→ 打开 /interactive-classroom/<classroom_id>
→ 浏览来自 PPT Studio 的 SVG slide
→ 做 quiz
→ 看到得分和解析
```

建议完成文件：

```text
backend/interactive_classroom/schema.py
backend/interactive_classroom/storage.py
backend/interactive_classroom/generator.py
backend/interactive_classroom/quiz_service.py
backend/app.py
frontend/src/api/interactiveClassroom.ts
frontend/src/types/classroom.ts
frontend/src/views/InteractiveClassroomPlayerView.vue
frontend/src/components/classroom/*
frontend/src/router.ts
```

### M2：TTS 主链路接入

目标：

- `speech Action` 自动生成音频；
- 前端自动播放；
- 支持用户选择 provider；
- Edge TTS 作为默认推荐。
- 复用微课视频已适配的 TTS provider 分发逻辑。

验收：

```text
生成课堂后，每个 speech action 有 audio_url
前端点击播放时能听到讲解
切换 provider 后仍走统一接口
```

### M3：课堂生成接入现有内容能力

目标：

- slide 来自现有 PPT Studio / SVG PPT pipeline；
- quiz 可来自现有测验生成器或题库；
- speech 可由 LLM 生成；
- 课堂结构更自然。

验收：

```text
换一个主题后，课堂内容、讲解和题目会随主题变化
```

### M4：学生画像驱动差异化课堂

目标：

- 增加轻量画像；
- 课堂生成读取画像；
- 同一主题下不同学生生成不同课堂。

验收：

```text
学生 A：零基础，生成基础讲解 + 简单题
学生 B：项目导向，生成代码案例 + 实践题
学生 C：竞赛导向，生成进阶讲解 + 边界题
```

### M5：学习记录与简要报告

目标：

- 保存答题结果；
- 汇总正确率；
- 标记薄弱知识点；
- 显示下一步建议。

验收：

```text
完成课堂后出现报告
报告能说明哪些知识点掌握较弱
系统能推荐下一节或补强课堂
```

### M6：学习路径串联多个课堂

目标：

- 生成课程阶段；
- 阶段卡片关联课堂；
- 完成一节后推荐下一节；
- 薄弱项插入补强阶段。

验收：

```text
画像 → 路径 → 课堂 → 答题 → 报告 → 下一阶段
```

---

## 13. 推荐实施顺序

### Step 0：执行前同步最新代码

真正开始实现前，必须先执行：

```text
git pull
```

要求：

- 只同步最新代码，不顺手改其他文件；
- 如遇冲突，先停止并报告；
- 不自动 commit；
- 不自动 push。

### Step 1：梳理可复用链路

先确认 PPT Studio 和微课视频可复用边界。

产出：

- 明确 `ppt_job_id → slides` 的读取方式；
- 明确 `speech text → audio_url` 的 TTS 复用方式；
- 明确课堂生成是否直接调用 `ppt_engine.pipeline`，还是先只包装已有 `ppt_job_id`。

### Step 2：后端课堂模型和存储

先写最小数据结构和 JSON 存储。

产出：

- `schema.py`
- `storage.py`
- 一个可读写的 `classroom.json`

### Step 3：课堂生成接口

先支持从已有 PPT Studio job 包装课堂，再支持从主题直接生成。

产出：

- `POST /api/interactive-classroom/generate`
- `GET /api/interactive-classroom/<id>`
- `GET /api/interactive-classroom/list`
- `ppt_job_id` 可选参数

### Step 4：PPT Studio 入口

在 PPT Studio 预览或历史记录中增加“转为交互式课堂”入口。

产出：

- 从 `job_id` 调用交互式课堂生成接口；
- 生成成功后跳转 `/interactive-classroom/:classroomId`；
- 不影响原有 PPT 下载、PPTist 编辑、微课生成。

### Step 5：前端课堂播放器

把真实接口数据渲染出来。

产出：

- `/interactive-classroom/:classroomId`
- `SlideScene`
- `QuizScene`
- `ActionNarrator`
- `ClassroomControls`

### Step 6：测验提交接口

让答题结果走后端。

产出：

- `POST /api/interactive-classroom/<id>/answer`
- 评分、解析、反馈 action

### Step 7：TTS 接入

接现有 TTS provider，优先复用微课视频里的 TTS 分发代码。

产出：

- `tts_service.py`
- `speech.audio_url`
- 前端自动播放

### Step 8：接现有生成器

把固定课堂升级成主题生成课堂。

产出：

- 讲解词随主题生成；
- 题目随主题生成；
- slide 支持 SVG 或更完整 PPT pipeline。

### Step 9：画像与差异化

加入轻量学生画像。

产出：

- `profile_engine`
- 画像设置页；
- 画像注入课堂生成。

### Step 10：报告与路径

最后补闭环。

产出：

- 学习报告；
- 掌握度更新；
- 路径页面；
- 下一阶段推荐。

---

## 14. 与现有模块的关系

### 14.1 与 PPT Studio

第一版交互式课堂应该优先复用 PPT Studio，而不是另做一套 PPT 模块。

推荐关系：

```text
PPT Studio 负责生成高质量 SVG slide
交互式课堂负责把 SVG slide 组织成可播放、可答题、可记录的课堂
PPTist 负责课堂 slide 的精修入口
```

入口建议：

- `PptStudioView` 生成完成后增加“转为交互式课堂”；
- `PreviewStage.vue` 增加课堂入口；
- `HistoryDrawer.vue` 对历史 PPT job 增加课堂入口；
- 文件库里的 SVG PPT 也可增加课堂入口。

后续增强：

- 课堂 slide 可复用 `ppt_engine.pipeline` 的 SVG 生成；
- 课堂结束后可导出为 PPT；
- PPTist 可作为编辑器，而不是课堂播放核心。

除非后续能做出明显优于 PPT Studio 的课堂专用 PPT 生成链路，否则不重写 PPT 模块。

### 14.2 与 QuizPlayer

现有 `QuizPlayer.vue` 可复用 UI 思路，但建议为课堂新建 `QuizScene.vue`，避免影响聊天侧文档预览。

后续可以把两者抽象为统一 Quiz 组件。

### 14.3 与 Video Studio

交互式课堂不是微课视频，但可以共享：

- TTS provider；
- 讲稿生成；
- PPT/SVG 页面；
- 后续可一键把课堂导出为微课视频。

具体复用点：

- `VideoGenerator` 的 TTS provider 分发；
- 微课视频逐页讲稿生成的 prompt 思路；
- 视频任务状态轮询的前端 store 思路；
- `generated_videos` 的用户隔离目录设计。

需要避免：

- 不把交互式课堂直接做成视频；
- 不复用视频生成的长任务模型作为课堂第一版必需项；
- 不让课堂播放依赖 ffmpeg。

### 14.4 与 Memory

第一版保存到 `backend/memory/users/<user_id>/`，与现有用户记忆体系保持一致。

### 14.5 与文件库

后续可以把生成的交互式课堂显示在文件库中，类型为：

```text
interactive_classroom
```

---

## 15. 演示样例设计

建议准备一个强演示主题：

```text
Python 循环语句
```

原因：

- 易懂；
- 适合讲解；
- 适合答题；
- 能体现不同学生差异；
- 能做代码案例。

### 15.1 示例课堂结构

```text
Scene 1：课程导入：为什么需要循环
Scene 2：while 循环：条件驱动的重复
Scene 3：随堂测验：while 条件判断
Scene 4：for 循环与 range
Scene 5：随堂测验：range 边界
Scene 6：break / continue
Scene 7：总结与下一步建议
```

### 15.2 三类学生对比

学生 A：零基础

```text
讲解：生活类比多
题目：基础概念题
语速：偏慢
反馈：鼓励 + 补基础
```

学生 B：项目导向

```text
讲解：多代码例子
题目：调试和输出判断
语速：正常
反馈：建议做小项目
```

学生 C：竞赛导向

```text
讲解：边界条件和效率
题目：嵌套循环、break/continue 组合
语速：略快
反馈：推荐进阶练习
```

---

## 16. 答辩叙事

最终答辩不要说“我们能生成 PPT、讲义、测验、视频”。应该说：

> 智创空间将传统 AI 内容生成升级为可交互的个性化学习课堂。系统围绕学生画像生成适配课堂，在课堂中通过 TTS 讲解和随堂测验完成学习互动，并根据答题结果更新学习报告和下一步推荐，形成“生成资源 → 学习互动 → 效果评估 → 动态调整”的闭环。

演示路径：

```text
1. 进入交互式课堂生成页
2. 输入主题“Python 循环语句”
3. 选择 TTS provider，默认 Edge TTS
4. 生成课堂
5. 进入课堂播放器
6. 听 AI 教师讲解
7. 完成随堂测验
8. 查看即时反馈
9. 查看学习报告
10. 展示不同学生画像下课堂差异
```

---

## 17. 风险与降级

| 风险 | 降级方案 |
| --- | --- |
| PPT SVG pipeline 接入耗时 | 第一版先包装已有 PPT job，不自动新生成 PPT |
| TTS provider 失败 | 自动降级到 Edge TTS 或文本讲解 |
| LLM 生成课堂不稳定 | 先用课程 JSON + 模板确定性生成 |
| Quiz 格式不稳定 | 第一版只支持单选题 |
| 学习路径来不及 | 先做单课堂报告，路径后补 |
| 页面复杂度过高 | 先做单页面播放器，不做复杂布局 |

---

## 18. 最小可验收版本

最小版本必须满足：

```text
1. 能生成一节真实交互式课堂
2. 能打开课堂子页面
3. 能复用 PPT Studio 的 SVG slide
4. 能播放教师讲解
5. 能调用 TTS 播放音频
6. 能完成随堂测验
7. 能提交答案并看到解析
8. 后端能保存课堂和答题记录
```

如果这 7 点完成，就已经不是前端壳子，而是一个真实可跑的交互式课堂系统。

---

## 19. 后续增强方向

完成最小闭环后，再考虑：

1. 接入完整学生画像；
2. 接入学习路径规划；
3. 接入 PPT SVG 多 Agent 生成；
4. 接入微课视频导出；
5. 接入知识图谱；
6. 接入 RAG 答疑；
7. 接入错题本；
8. 接入教师端课程管理；
9. 接入 OpenMAIC 风格的多 Agent 讨论；
10. 接入白板和互动 widget。

---

## 20. 结论

本阶段最重要的不是把所有页面一次铺开，而是先做出一个真实、可播放、可答题、可反馈、可记录的交互式课堂。

推荐主线：

```text
同步最新 Git 代码
→ 梳理 PPT Studio / 微课视频复用边界
→ 从已有 PPT job 包装交互式课堂
→ TTS 主链路接入
→ 主题生成课堂
→ 学生画像差异化
→ 学习报告
→ 学习路径闭环
```

这样既能吸收 OpenMAIC 最有价值的课堂交互思想，又能充分利用智创空间现有的 Flask + Vue3 + SVG PPT + Quiz + TTS 技术基础，避免大规模重构，同时尽快形成一个能打动评委的核心演示体验。
## 21. 当前开发进度（2026-06-01）

### 21.1 已完成

- 已完成后端最小交互式课堂模块接入：
  - `backend/interactive_classroom/schema.py`
  - `backend/interactive_classroom/storage.py`
  - `backend/interactive_classroom/generator.py`
  - `backend/interactive_classroom/quiz_service.py`
  - `backend/interactive_classroom/tts_service.py`
- 已在 `backend/app.py` 接入第一批真实接口：
  - `POST /api/interactive-classroom/generate`
  - `GET /api/interactive-classroom/list`
  - `GET /api/interactive-classroom/<classroom_id>`
  - `POST /api/interactive-classroom/<classroom_id>/answer`
  - `GET /api/interactive-classroom/<classroom_id>/audio/<filename>`
- 已完成前端最小页面链路：
  - `frontend/src/views/InteractiveClassroomHomeView.vue`
  - `frontend/src/views/InteractiveClassroomPlayerView.vue`
  - `frontend/src/api/interactiveClassroom.ts`
  - `frontend/src/router.ts` 新增课堂入口与播放器路由
- 已接入 PPT Studio 到交互式课堂的第一版复用能力：
  - 在 `frontend/src/views/PptStudioView.vue` 增加“转课堂”入口
  - 支持基于当前 `topic + ppt_job_id` 生成课堂

### 21.2 本轮 review 后已修复

- 修复课堂文案乱码与提示语异常问题
- 修复课堂音频 URL 重复拼接 `user_id` 的问题
- 修复 Edge TTS 产物被错误保存为 `.wav` 的问题，现按 provider 输出正确扩展名
- 修复音频接口固定返回 `audio/wav` 的问题，现按文件类型自动返回 MIME
- 补上答题后的结果展示与 `quiz_feedback` 注入，播放器现在会把反馈真正挂回当前场景

### 21.3 当前状态判断

- 目前已经不是纯前端壳子，而是“后端可生成课堂 + 前端可进入课堂 + 可答题 + 可回显反馈”的第一版闭环
- 但还没有完成“学习报告 / 画像驱动 / 路径推荐”这几个后续闭环模块
- 课堂内容生成目前仍偏演示型，题目和讲解词还没有充分接入现有内容生成器

### 21.4 下一步建议

1. 补 `report` 接口与课堂完成页，形成“生成 → 学习 → 答题 → 报告”闭环
2. 让 quiz 数据优先复用现有 `quiz_generator.py` 或题库，而不是继续写死示例题
3. 让 slide 讲解词和课堂结构更深地复用现有 PPT Studio / 微课视频能力
4. 再开始接轻量学生画像，避免过早做前端包装页
## 22. 测试反馈与下一轮执行重点（2026-06-01）

### 22.1 当前测试反馈

- 交互式课堂已经可以生成对应页面，并且每页可以生成音频。
- 现阶段的页面讲解词仍是 MVP 占位逻辑，大多数 PPT 页面都会生成类似“现在我们看第 x 页，这一页我带你抓住最关键的信息点。”的通用讲解。
- 随堂测验页面目前只是最小可用 UI，能选题、提交、查看结果，但还没有达到 OpenMAIC 式课堂互动体验。
- 因此前一轮实现应被视为“主链路打通”，不是最终课堂体验。

### 22.2 下一轮优先级调整

下一步不急着继续铺画像页或路径页，而是先把“课堂本身”变得像一个真实产品：

1. 升级 slide 讲解词生成：
   - 优先从现有 PPT SVG / metadata / topic 中抽取每页标题与主要文本。
   - 为每一页生成差异化讲解词，而不是固定模板话术。
   - 后续再接 LLM 生成更自然的教师讲稿。

2. 升级课堂播放器 UI：
   - 左侧场景列表保留，但增强当前进度、slide / quiz 类型标识。
   - 中间区域优化 SVG 缩放、测验卡片、答题反馈、解析展示。
   - 底部讲解区做成更像“AI 教师讲解面板”，支持播放、暂停、重播和文本展开。

3. 升级 QuizScene：
   - 不再停留在原生 radio + 简单结果文本。
   - 增加选项状态、正确/错误标识、逐题解析、得分总结。
   - 后续复用或抽象现有 `QuizPlayer.vue` 的成熟交互能力。

4. 再补学习报告：
   - 在课堂体验明显改善后，再做 `report` 接口与完成页。
   - 报告基于答题结果、知识点、薄弱项生成，承接后续学生画像和学习路径。

### 22.3 下一步执行任务

推荐下一轮直接执行：

```text
M1.5：提升交互式课堂可用性
→ 从 PPT SVG 中提取每页文本
→ 生成每页差异化 speech action
→ 改造播放器 UI
→ 改造 quiz 反馈展示
→ 再进入 report 闭环
```

### 22.4 本轮执行进度

- 已升级课堂生成器的讲解词来源：
  - 优先读取 PPT job 目录中的 `manuscript.md`，把已有 PPT 讲稿映射为每页 `speech action`。
  - 当没有 `manuscript.md` 时，从 SVG 中提取可见文本，生成基于标题和要点的差异化讲解词。
  - 已兼容新版 `generated_svg_ppt/users/<user_id>/<job_id>` 与旧版 `generated_svg_ppt/<job_id>` 两种 PPT 产物目录。
- 已升级 fallback 课堂和测验占位内容：
  - 不再使用固定 for/range 示例题作为所有主题的默认题。
  - fallback quiz 改为围绕当前 topic 的理解、复听、反馈闭环。
- 已升级交互式课堂播放器 UI：
  - 左侧场景列表增加当前进度、场景类型和更稳定的列表布局。
  - 顶部增加上一页 / 下一页导航。
  - 测验区改为卡片式选项，提交后显示正确项、错误项、逐题解析和得分面板。
  - 底部讲解区调整为 AI 教师讲解面板，保留音频播放。

### 22.5 当前剩余问题

- 讲解词现在已经能做到“每页不同”，但仍以确定性提取和已有讲稿复用为主，还没有接入 LLM 做更自然的二次润色。
- Quiz 题目已经不再完全固定，但还没有接入现有 `quiz_generator.py` 或正式题库。
- 课堂完成后的学习报告还未实现，下一步仍需要补 `report` 接口和完成页。

### 22.6 本轮 review 修复

- 修复答题反馈 TTS 配置不完整的问题：反馈音频现在会重新读取用户设置、provider 默认 base_url 和服务端环境变量中的 API Key，不再只依赖课堂 JSON 中的 `provider/model/voice`。
- 修复播放器 quiz 状态只存单份的问题：现在答题答案和提交结果按 `scene_id` 分桶保存，为后续一节课插入多个测验做准备。

### 22.7 自动播放与自动翻页补强

- 播放器新增自动播放开关，默认开启，顶部按钮可切换为手动播放。
- `speech` 音频播放结束后会自动进入下一页，让课堂更接近连续讲解体验。
- 如果当前讲解页没有音频，会以文本页降级策略停留约 5 秒后进入下一页。
- 遇到 `quiz` 场景时自动播放会暂停，避免学生未答题就被自动带走；答题完成后仍由用户手动继续，后续可再做“答完反馈音频播放完自动继续”的可选模式。

### 22.8 提交前 review 修复

- 补充 `ppt_job_id` 格式校验，避免课堂生成接口被异常路径参数污染。
- 收紧 `ClassroomStorage` 的 `user_id/classroom_id` 校验，并避免读取不存在课堂时创建空目录。
- 补齐交互式课堂生成请求中的 `tts_api_key/tts_base_url` 字段，PPT Studio 和课堂首页都会传入当前 TTS provider 的有效配置，保持与微课视频生成链路一致。
